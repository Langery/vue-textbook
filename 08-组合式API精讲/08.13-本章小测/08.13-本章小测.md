# 8.13 本章小测

> 章节：第 8 章 组合式 API 精讲
> 课时：0.5 课时

---

## 一、单选题（每题 5 分，共 30 分）

**1. 关于组合式 API 和选项式 API，以下说法正确的是？**

A. 组合式 API 完全替代了选项式 API，Vue 3 不能写选项式代码
B. 组合式 API 的核心优势是按"功能"组织代码，方便逻辑复用
C. `<script setup>` 里不能 import Vue 的 API，必须全局注册
D. 选项式 API 可以在同一个功能内部把代码聚在一起

**2. 以下关于 ref 的说法，错误的是？**

A. `ref` 可以包装对象，内部会调用 `reactive`
B. `shallowRef` 只追踪 `.value` 的替换，不追踪内部属性变化
C. `triggerRef` 可以强制触发 `reactive` 对象的视图更新
D. `toRef(reactiveObj, 'attr')` 创建的是一个和原属性双向绑定的 ref

**3. 以下哪个是 computed 的正确描述？**

A. computed 可以执行异步操作，适合发网络请求
B. computed 有缓存机制，依赖不变时返回缓存结果
C. computed 和 methods 完全一样，只是写法不同
D. 可写 computed 是通过直接给 `computedRef.value` 赋值实现的

**4. provide / inject 最不适合以下哪种场景？**

A. 全局主题切换
B. 表单组件内部通信（Form → FormItem）
C. 跨路由的全局状态共享
D. 深层嵌套组件传递用户权限信息

**5. 关于自定义指令，以下说法正确的是？**

A. 指令可以替代组件，所有 UI 功能都应该用指令
B. 以 `v` 开头的驼峰变量（如 `vFocus`）在模板中自动映射为 `v-focus`
C. 指令的 `mounted` 钩子和组件的 `onMounted` 执行时机完全不同
D. 指令只能全局注册，不能局部注册

**6. 关于 composable（组合式函数），以下说法错误的是？**

A. composable 函数名约定以 `use` 开头
B. composable 可以在 `if` 语句中调用
C. composable 里可以注册生命周期钩子
D. composable 的每个调用实例是独立的

---

## 二、判断题（每题 3 分，共 15 分）

**7.** `shallowRef` 修改内部属性后，必须用 `triggerRef` 才能触发视图更新。 ( )

**8.** `readonly(reactiveObj)` 创建的只读对象在开发模式下改了会报警告，但不会阻止修改。 ( )

**9.** 在 `computed` 的回调函数里修改其他响应式数据是推荐做法。 ( )

**10.** `toRefs` 可以把 `reactive` 对象的每个属性转成独立的 `ref`，解构后不丢响应式。 ( )

**11.** VueUse 是 Vue 官方核心库的一部分，不需要额外安装。 ( )

---

## 三、简答题（每题 10 分，共 30 分）

**12.** 请用自己的话解释：computed、watch、methods 三者的使用场景如何区分？各举一个具体的代码例子。

**13.** 什么是 Prop Drilling？provide / inject 如何解决它？使用 provide / inject 时为什么要搭配 `readonly`？请给出代码示意。

**14.** 有同学写了下面这段 composable，请帮他找出 3 个问题并修正：

```js
// composables/useFetch.js
import { ref } from 'vue'

export function useFetch(url) {
  const data = ref(null)
  const loading = ref(false)

  if (url) {
    loading.value = true
    fetch(url)
      .then(res => res.json())
      .then(json => data.value = json)
      .finally(() => loading.value = false)
  }

  return { data, loading }
}
```

---

## 四、编程题（共 25 分）

**15. （10 分）** 用自定义指令实现 `v-lazy`：图片懒加载。当图片进入可视区域（viewport）时才加载真实的 `src`。提示：使用 `IntersectionObserver`。

**16. （15 分）** 实现一个完整的搜索组件，要求：

- 用 composable（`useSearch`）封装搜索逻辑
- 用 `useDebounce` 做输入防抖（500ms）
- 点击搜索结果可以复制链接（用自定义指令 `v-copy`）
- 搜索输入框自动聚焦（用自定义指令 `v-focus`）

---

## 参考答案

**一、单选题**

| 题号 | 答案 | 解析 |
|---|---|---|
| 1 | B | 组合式 API 的核心价值是逻辑组合与复用 |
| 2 | C | `triggerRef` 只对 `shallowRef` 有效，`reactive` 不需要手动触发 |
| 3 | B | computed 有缓存，依赖不变直接返回缓存 |
| 4 | C | 跨路由全局状态应该用 Pinia |
| 5 | B | `vFocus` → `v-focus` 是 Vue 的自动映射规则 |
| 6 | B | composable 必须在 setup 顶层同步调用 |

**二、判断题**

| 题号 | 答案 |
|---|---|
| 7 | ✅ 正确 |
| 8 | ✅ 正确 |
| 9 | ❌ 错误（computed 应该是纯函数，不应有副作用） |
| 10 | ✅ 正确 |
| 11 | ❌ 错误（VueUse 是独立社区库，需 `npm install @vueuse/core`） |

**三、简答题**

**12.** 参考答案：
- **computed**：基于已有数据算出一个新值，有缓存。例：`const total = computed(() => price.value * quantity.value)`
- **watch**：监听某个数据变化后"做一件事"（副作用）。例：`watch(keyword, (v) => fetch('/api?q=' + v))`
- **methods**：用户触发时执行。例：`function handleSubmit() { ... }`

**13.** 参考答案：
- Prop Drilling：祖先的数据需要通过 Props 一层层传给深层后代，中间组件被迫转发不关心的数据。
- provide/inject 让祖先直接注入数据，后代直接接收，跳过中间层。
- 搭配 `readonly` 是防止后代组件直接修改祖先数据，破坏单向数据流。代码：`provide('state', readonly(reactiveState))`。

**14.** 三个问题：
1. 在 composable 顶层使用 `if` 语句发请求 —— composable 不应该在非响应式的条件中执行副作用，应该用 `watch` 监听 url 变化。
2. 没有用 `onMounted` 或 `watch` 包裹 fetch —— fetch 会立即执行，不利于控制时机。
3. 没有错误处理 —— 应加 `try/catch` 或 `.catch`，并暴露 `error` 状态。

修正版本：
```js
import { ref, watch } from 'vue'

export function useFetch(url) {
  const data = ref(null)
  const loading = ref(false)
  const error = ref(null)

  watch(() => url, async (newUrl) => {
    if (!newUrl) return
    loading.value = true
    error.value = null
    try {
      const res = await fetch(newUrl)
      data.value = await res.json()
    } catch (e) {
      error.value = e.message
    } finally {
      loading.value = false
    }
  }, { immediate: true })

  return { data, loading, error }
}
```

**四、编程题**

**15.** `v-lazy` 参考实现：

```js
export const vLazy = {
  mounted(el, binding) {
    const observer = new IntersectionObserver(([entry]) => {
      if (entry.isIntersecting) {
        el.src = binding.value   // 进入可视区，加载真实 src
        observer.unobserve(el)   // 加载后停止观察
      }
    })
    observer.observe(el)
    el._lazyObserver = observer  // 存下来以便清理
  },
  unmounted(el) {
    el._lazyObserver?.disconnect()
  }
}
```

**16.** 参考架构：

```
SearchBox.vue
├── useSearch.js (composable)
│   ├── keyword (ref)
│   ├── results (ref)
│   ├── loading (ref)
│   └── search() 
├── useDebounce(keyword, 500) → debouncedKeyword
├── v-focus on input
└── v-copy on result links
```

---

**（8.13 本章小测结束，下一站 8.14 章末实战：本地笔记 →）**
