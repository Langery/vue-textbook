# 6.10 本章小测

> 章节：第 6 章 生命周期与 watchEffect
> 分值：100 分（60 分合格）

---

## 📋 一、选择题（每题 5 分，共 35 分）

### 1. Vue 组件挂载到 DOM 后，哪个生命周期钩子会被调用？

- A. `setup()`
- B. `onBeforeMount`
- C. `onMounted`
- D. `onUpdated`

### 2. 以下哪个操作属于"副作用"？

- A. `const sum = a + b`
- B. `const total = computed(() => price.value * qty.value)`
- C. `fetch('/api/user')`
- D. `const greeting = 'Hello, ' + name`

### 3. `watchEffect` 与 `watch` 的区别，正确的是？

- A. `watchEffect` 可以拿到 `oldVal`，`watch` 不行
- B. `watch` 默认立即执行，`watchEffect` 不会
- C. `watchEffect` 自动追踪依赖，`watch` 需手动指定源
- D. 两者完全一样，只是名字不同

### 4. 以下代码中，修改 `list.value[0]` 能否触发 watch 回调？

```javascript
const list = ref([1, 2, 3])
watch(list, () => console.log('变了'))
```

- A. 能，默认 deep: true
- B. 不能，ref 数组默认不 deep，需要 `{ deep: true }`
- C. 能，ref 数组默认就是 deep
- D. 不能，ref 数组不支持 watch

### 5. Vue 3.5 中，在 `watch` 回调里注册清理函数应该用哪个 API？

- A. `onCleanup`
- B. `onBeforeUnmount`
- C. `onWatcherCleanup`
- D. `onScopeDispose`

### 6. 以下哪个场景**不该**用 `watch`？

- A. `userId` 变了，重新请求用户详情
- B. 用户点击"搜索"按钮，发起搜索请求
- C. 搜索关键词变了，500ms 防抖后请求
- D. 表单数据变了，自动保存到 localStorage

### 7. `effectScope` 的主要用途是？

- A. 替代 Vue 组件的生命周期
- B. 批量创建和管理副作用，支持一次性停止
- C. 替代 `watchEffect`
- D. 监听组件外部的变量

---

## ✍️ 二、填空题（每题 5 分，共 25 分）

### 8. Vue 3 组件生命周期的四个阶段是：\_\_\_\_\_\_、\_\_\_\_\_\_、\_\_\_\_\_\_、\_\_\_\_\_\_。

### 9. `watchEffect` 默认的执行时机是组件渲染 \_\_\_\_\_\_（前/后），可以用 `flush: 'post'` 改为渲染后执行。

### 10. 监听 `ref` 包装的数组的 `push` 操作，需要在 watch 的第三个参数中设置 `{ ______: true }`。

### 11. `effectScope` 中，注册"作用域销毁时的清理回调"使用的 API 是：`______________`。

### 12. 在 `watch` 的回调参数中，第一个参数是 \_\_\_\_\_\_，第二个参数是 \_\_\_\_\_\_。

---

## 🛠 三、代码题（共 40 分）

### 13. 填空题（代码补全）（10 分）

补全以下代码，实现"搜索输入框 500ms 防抖"：

```vue
<script setup>
import { ref, watch, _______________ } from 'vue'

const keyword = ref('')
const result = ref('')

watch(keyword, (newVal) => {
  const timer = setTimeout(() => {
    result.value = `搜索「${newVal}」的结果`
  }, 500)

  _______________(() => {
    _______________
  })
})
</script>
```

### 14. 实战题（20 分）

写一个 Vue 3 组件 `StayTimer`，实现以下功能：

1. 组件挂载时，开始计时（每秒 +1）
2. 页面上显示"已停留：XX 秒"
3. 组件卸载时，`console.log` 输出总停留时间
4. 使用 `<script setup>` 语法

**要求**：
- 使用 `onMounted` 启动定时器
- 使用 `onUnmounted` 清理定时器
- 使用 `ref` 管理计时变量

### 15. 分析题（10 分）

以下代码存在什么问题？请指出至少 2 个问题，并写出修复后的代码。

```
<script setup>
import { ref, watch } from 'vue'

const userId = ref(1)
const userData = ref(null)

watch(userId, async (newId) => {
  const res = await fetch(`/api/user/${newId}`)
  userData.value = await res.json()
})
</script>

<template>
  <button @click="userId++">下一个用户</button>
  <pre>{{ userData }}</pre>
</template>
```

---

## 📊 参考答案

### 选择题
1. C  2. C  3. C  4. B  5. C  6. B  7. B

### 填空题
8. 创建 挂载 更新 卸载
9. 前
10. deep
11. onScopeDispose
12. 新值（newVal） 旧值（oldVal）

### 代码题

**13. 代码补全**：

```javascript
import { ref, watch, onWatcherCleanup } from 'vue'

watch(keyword, (newVal) => {
  const timer = setTimeout(() => {
    result.value = `搜索「${newVal}」的结果`
  }, 500)

  onWatcherCleanup(() => {
    clearTimeout(timer)
  })
})
```

**14. 实战题参考**：

```vue
<script setup>
import { ref, onMounted, onUnmounted } from 'vue'

const seconds = ref(0)
let timer = null

onMounted(() => {
  timer = setInterval(() => {
    seconds.value++
  }, 1000)
})

onUnmounted(() => {
  clearInterval(timer)
  console.log(`用户停留了 ${seconds.value} 秒`)
})
</script>

<template>
  <p>已停留：{{ seconds }} 秒</p>
</template>
```

**15. 分析题参考**：

问题 1：**竞态条件**——用户快速点击，旧请求可能覆盖新请求。
问题 2：**没有清理**——组件卸载时请求可能还在进行。

修复：

```
<script setup>
import { ref, watch, onWatcherCleanup } from 'vue'

const userId = ref(1)
const userData = ref(null)
const loading = ref(false)

watch(userId, async (newId) => {
  loading.value = true
  const controller = new AbortController()

  onWatcherCleanup(() => {
    controller.abort()
  })

  try {
    const res = await fetch(`/api/user/${newId}`, {
      signal: controller.signal
    })
    userData.value = await res.json()
  } catch (err) {
    if (err.name !== 'AbortError') {
      console.error(err)
    }
  } finally {
    loading.value = false
  }
})
</script>
```

---

## 🏆 评分标准

| 分数 | 等级 | 建议 |
|---|---|---|
| 90-100 | 优秀 ⭐⭐⭐ | 直接进入第 8 章 |
| 75-89 | 良好 ⭐⭐ | 重点复习错题涉及的小节 |
| 60-74 | 合格 ⭐ | 重新阅读丢分的小节 |
| 0-59 | 需加强 | 建议重读 6.3、6.4、6.5 节 |

---

**（6.10 本章小测结束，下一站 6.11 章末实战：番茄钟 →）**（注：原"第 7 章 条件列表与 key"已并入第 2 章，故无独立第 7 章）
