---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: a2bf0a7a8afbd1fb49ac51912b613772_0888ec9dac2311f1af37525400826444
    ReservedCode1: H0pKZfKqYQR3uSvTNs5RoItQOosGuWksJRK33tPi3s0mGsTuWAr6i4aI9yHvccuVhJc7Y3ZuqV1K61XNcUtZCXsZ0prfnS3hOEgSXvnjBOZYscnQ7f73zwNDcIWx5gVKIa87lERpLsiwqdNMI9qCIqIBxDVEBDiozxOsyqAMzd3BBhKntrkyQMgWC5g=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: a2bf0a7a8afbd1fb49ac51912b613772_0888ec9dac2311f1af37525400826444
    ReservedCode2: H0pKZfKqYQR3uSvTNs5RoItQOosGuWksJRK33tPi3s0mGsTuWAr6i4aI9yHvccuVhJc7Y3ZuqV1K61XNcUtZCXsZ0prfnS3hOEgSXvnjBOZYscnQ7f73zwNDcIWx5gVKIa87lERpLsiwqdNMI9qCIqIBxDVEBDiozxOsyqAMzd3BBhKntrkyQMgWC5g=
---

# 6.11 章末实战：番茄钟（P6）

> 章节：第 6 章 生命周期与 watchEffect
> 课时：1 课时
> 类型：章末实战 · 独立小项目 P6

---

第六章回答了 Vue 里"什么时候、做什么事"：生命周期钩子管时机，`watchEffect` / `watch` 管响应，清理函数管善后。这一节我们用它们做一个**真正靠"时间副作用"跑起来**的应用——「番茄钟 + 学习记录器」。它和大项目 Campus2Hand 的关联很直接：拍卖/抢购倒计时、订单状态轮询，底层都是"定时副作用 + 生命周期清理"这套写法。先在番茄钟上把它练熟。

---

## 🎯 项目需求（一句话）

做一个"番茄钟 + 学习记录器"：25 分钟专注倒计时，结束后自动进入 5 分钟休息并记录一个"完成番茄"；支持开始 / 暂停 / 重置，界面剩余秒数实时刷新——全程只用第 6 章已学的生命周期、`watchEffect` / `watch`、定时副作用与清理，不引入任何新库。

### ✅ 验收标准（能跑、能展示）

- [ ] 项目能 `npm run dev` 启动，能看到一个大大的倒计时和"专注 / 休息"状态
- [ ] 点"开始"后每秒倒计时，剩余时间实时更新；到 0 自动切到休息模式并提示
- [ ] 点"暂停"能停住计时，"重置"能回到初始 25:00
- [ ] 每完成一个完整专注周期，右上角"今日完成番茄" +1
- [ ] 页面标题（`document.title`）能跟着显示当前剩余时间（用副作用实现）
- [ ] 能向同学讲清：定时器放在哪个钩子里创建、为什么必须在卸载时清理（切走页面后时间不能再跑）

---

## 🛠 分步提示（3~5 步，含关键代码锚点）

> 以下只给路线和关键锚点，**不贴全量答案**。卡住时回头翻 6.1 / 6.3 / 6.4 / 6.5 / 6.6 对应小节。

### Step 1：设计"状态机"

番茄钟有两种模式：`focus`（25 分钟）与 `break`（5 分钟）。用 `reactive`（或 `ref`）存 `mode`、`remainSeconds`、`running`、`finishedCount` 四个状态。

### Step 2：开始/暂停——用 `watchEffect` 管理定时器（回顾 6.3 / 6.5）

关键思路：让定时器的创建/清理**跟着 `running` 状态走**，用 `watchEffect` 在副作用里开停：

```js
watchEffect(() => {
  if (!state.running) return
  const timer = setInterval(() => {
    if (state.remainSeconds > 0) {
      state.remainSeconds--
    }
  }, 1000)
  return () => clearInterval(timer)   // watchEffect 里返回清理函数，重跑前自动清理
})
```

> 锚点检查：点"开始"后每秒递减；点"暂停"后立即停住；再次"开始"能续跑——注意 watchEffect 重跑时旧定时器被自动清掉了，不会叠出多个计时器。

### Step 3：到 0 切换模式 + 计数（回顾 6.4 watch）

用 `watch` 监听 `remainSeconds` 归零的那一刻，而不是在定时器里到处判断：

```js
watch(
  () => state.remainSeconds,
  (val) => {
    if (val !== 0) return
    if (state.mode === 'focus') {
      state.finishedCount++
      state.mode = 'break'
      state.remainSeconds = BREAK_SECONDS
    } else {
      state.mode = 'focus'
      state.remainSeconds = FOCUS_SECONDS
    }
    state.running = false   // 一轮结束，等用户再点开始
  }
)
```

### Step 4：同步页面标题——用副作用改"外部世界"（回顾 6.2 / 6.3）

把剩余时间同步到 `document.title`，这正是副作用的典型场景（改的是 Vue 之外的东西）：

```js
watchEffect(() => {
  const mm = String(Math.floor(state.remainSeconds / 60)).padStart(2, '0')
  const ss = String(state.remainSeconds % 60).padStart(2, '0')
  document.title = `${state.running ? '⏳' : '⏸'} ${mm}:${ss} · ${state.mode === 'focus' ? '专注中' : '休息中'}`
})
```

### Step 5：卸载时兜底清理 + 界面完善

即使 `watchEffect` 已负责清理，也建议在卸载钩子里显式把计时状态归零，形成"双保险"习惯（回顾 6.6）：

```js
onUnmounted(() => {
  state.running = false   // 触发 watchEffect 重跑 → 定时器被清理
})
```

界面用 `v-if / v-else` 显示专注/休息配色，把按钮（开始/暂停/重置）与"今日完成番茄"统计摆好。

---

## 🚀 延伸挑战（1~2 个加分项）

1. **声音 + 桌面通知提醒**：专注结束那一刻弹出系统通知（`new Notification('专注结束！')`），体验真实的"副作用"。
2. **学习记录列表**：每完成一个番茄就把"几点几分完成"追加到列表里，用 `watch` 监听 `finishedCount` 变化来记录——"副作用响应数据变化"的又一实例。

---

## 🔮 它在最后的大项目里长成什么

这一节的"番茄钟"，就是大项目 **Campus2Hand 校园二手集市**的**倒计时与状态同步**雏形：拍卖/抢购的"距结束 xx:xx"就是这里的 `remainSeconds` 倒计时，订单"等待支付/已超时"的状态流转就是这里的模式切换；第 13 章把 25 分钟换成拍卖截止时间，把专注计数换成订单状态轮询，你会觉得"这套写法我熟"。

---

下一章是**第 8 章"组合式 API 精讲"**——（原"第 7 章 条件列表与 key"已并入第 2 章，无独立第 7 章）自定义 composable 会大量用到 `watchEffect`。章末有 P8 在等你，先把本章副作用练扎实。

---

**（第 6 章结束 🎉 继续学习第 8 章 →）**（注：原"第 7 章 条件列表与 key"已并入第 2 章，故无独立第 7 章）
*（内容由AI生成，仅供参考）*
