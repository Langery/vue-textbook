# 第 10 章 服务端通信 ⭐

> 课时：8 课时
> 难度：⭐⭐⭐（进阶）
> 重要性：⭐⭐⭐⭐（**项目实战必备**）

---

## 🎯 本章目标

学完这章，你将能够：

- ✅ 理解**前后端分离架构**：API 是什么、前端怎么和后端"说话"
- ✅ 熟练使用 **Axios** 发送 GET / POST 请求
- ✅ 掌握**全局配置与拦截器**：独立实例、baseURL、token 注入、统一错误处理
- ✅ 掌握**错误处理与重试**：区分网络错误 / 超时错误 / 业务错误
- ✅ 用 **Composable 封装请求状态**（loading / error / data 三态）
- ✅ 用 **AbortController** 取消请求，防止内存泄漏

---

## 🤔 这章讲什么、为什么、学完能干什么

**这章讲什么？**

从第 9 章开始，你已经能搭出"长得像应用"的前端了。但一个正经的 Vue 项目，数据不是写死在组件里的——它来自后端服务器。本章教你**前端如何与后端通信**：HTTP 协议基础、Axios 请求库、请求拦截器、错误处理与请求状态管理，一条链路打通。

**为什么必须有这章？**

没有服务端通信，前端只是"静态页面"；有了它，你才能做登录注册、商品列表、订单管理这些真实业务。第 13 章综合项目的所有数据请求，全部依赖本章的 Axios 封装能力。

**学完能干什么？**

- 独立编写项目的 API 请求层（`api/index.js` + 各模块接口函数）
- 自动携带 token、统一弹错误提示，代码不再到处重复 try-catch
- 处理超时、断网、业务失败的场景，页面体验不崩

> 🧑 **小白**：目前我的页面数据都是写死的，怎么才能拿到后端数据库里的"真数据"？

> 🤖 **Vue 君**：通过 HTTP 接口！我用 Axios 帮你发请求、收响应，你只管把数据显示出来。

> 👨🏫 **老湿**：登录、列表、下单……真实业务全靠这一章。第 13 章综合项目的数据请求，也会复用这里的 Axios 封装。

> 💬 **老师备注**：第 10 章 + 第 13 章是"从会调接口到会做项目"的完整链路——本章把请求能力练扎实，综合项目就能直接复用。

---

## 📋 本章小节

| 节 | 标题 | 内容 | 课时 |
|---|---|---|---|
| 10.1 | 前后端分离是啥 | 前后端分离架构、HTTP 结构、API 概念 | 0.5 |
| 10.2 | Axios 入门 | 安装引入、`axios.get` / `axios.post`、async/await | 0.5 |
| 10.3 | GET 请求实战 | 列表 / 详情请求、`onMounted` 自动请求、loading/空/错误三态 | 1 |
| 10.4 | POST 请求实战 | 请求体写法、新增成功后更新列表、201 状态码 | 1 |
| **10.5** | **全局配置与拦截器** ⭐ | **独立实例、baseURL、token 注入、统一错误提示** | **1** |
| **10.6** | **错误处理与重试** ⭐ | **2xx/4xx/5xx 处理策略、三种错误类型、自动重试** | **1** |
| **10.7** | **请求状态管理** ⭐ | **useRequest 组合式封装、AbortController 取消请求** | **1** |
| 10.8 | 本章小测 | 选择 + 判断 + 编码题 | 0.5 |
| **10.9** | **章末实战：每日一言壁纸** ⭐ | axios 实例 + 拦截器 + useRequest 三态：一言 + 随机壁纸 | **1** |

---

## 🛠 本章实战

1. 用 Axios 写一个"新闻列表"页面：进入自动加载、下拉刷新重新请求
2. 封装 `api/index.js`：创建独立实例 + 请求/响应拦截器
3. 给所有请求自动注入 token，并在 401 时跳转登录页
4. 写一个 `useRequest` composable，组件里三行代码完成请求
5. 给请求加"失败自动重试 3 次"，并对 4xx 业务错误单独提示

---

## ⚠️ 学习提示

- **先理解再封装**：先学会裸写 `axios.get`，再学拦截器，最后才封装 `useRequest`——不要跳过基础直接抄封装代码。
- **关注拦截器的收益**：token 注入和统一错误处理放在拦截器里，是所有真实项目的标准姿势。
- **错误处理是加分项**：能区分"网络断、超时、业务失败"的工程师，写出的页面才叫"可用"。

---

## 🔗 与其他章的关系

- **前置**：第 9 章（路由与状态管理，特别是 Pinia 存 token）
- **后续直接被依赖**：第 13 章（综合项目所有接口请求都走本章封装的 Axios）

---

**（开始学习：10.1 前后端分离是啥 →）**
